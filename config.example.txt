# Скопировать в config.txt и заполнить реальными значениями.
# config.txt не коммитить / не пересылать — там рабочий webhook.

# URL списка заказов CRM (без параметров фильтра — они добавляются кодом)
CRM_ORDERS_URL=https://a.ok-crm.com/order/index

# Вебхук нового Apps Script (после развёртывания Code.gs, см. README)
GOOGLE_SHEET_WEBHOOK=https://script.google.com/macros/s/ЗАМЕНИТЬ_НА_СВОЙ/exec

# Через сколько дней после "Истекает" считать платёж просроченным
OVERDUE_DAYS=7

# Статусы платежа, которые считаются оплаченными (платёж НЕ считается просроченным)
PAID_STATUSES=Погашен,Оплачен,Неактивный

# Опционально: точный диапазон дат маршрута (приоритетнее, чем ROUTE_DATE_DAYS_BACK ниже).
# Формат — как в самой CRM: ДД.ММ.ГГГГ. Оба поля обязательны, если используете это.
ROUTE_DATE_FROM=
ROUTE_DATE_TO=

# Опционально: ограничить список заказов по "Дата маршрута" последними N днями
# (сузит выборку для регулярных прогонов, чтобы не перебирать всю историю
# каждый раз). Используется только если ROUTE_DATE_FROM/TO выше не заданы.
# Пусто = без ограничения (весь период, как сейчас для бэклога).
ROUTE_DATE_DAYS_BACK=
